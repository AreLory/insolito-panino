export default interface IProfile {
  fullName: string;
  email: string;
  phoneNumber?: string;
  address?: string;
}

export default interface User {
  _id:string
  fullName: string;
  email: string;
  phoneNumber?: string;
  address?: string;
}
